import pprint
import sys

from SC6_Camera import Logger
from SC6_Camera import Config

class FS():
    __init__(self, config)

    self.config = config

    l = Logger.Logger(config)
    self.logger = l.getLogger("DataStoreFS")
